[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "nodejs_22", "npm-9_x"]

[phases.build]
cmds = ["composer install --ignore-platform-reqs", "npm i", "npm run build"]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
